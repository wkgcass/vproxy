test {
    useJUnit()
    filter {
        includeTestsMatching 'io.vproxy.test.VSuite'
        includeTestsMatching 'io.vproxy.ci.CI'
    }
    testLogging.showStandardStreams(true)
    forkEvery = 1
}
