compileJava {
    doFirst {
        options.compilerArgs << '--module-path' << classpath.asPath
    }
    options.compilerArgs << '--add-modules' << [
        'kotlin.stdlib',
        'kotlinx.coroutines.core',
    ].join(',')
}

jar {
    archiveFileName = "vproxy-dep.jar"
}
