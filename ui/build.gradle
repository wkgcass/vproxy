compileJava {
    options.compilerArgs << '--add-modules' << [
        'io.vproxy.dep',
        'io.vproxy.base',
        'io.vproxy.lib',
        'io.vproxy.core',
        'io.vproxy.extended',
        'io.vproxy.app',
        'io.vproxy.all',
    ].join(',')
}

jar {
    archiveName "vproxy-ui.jar"
}
