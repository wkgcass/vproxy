/**
 * A very low level lib for java nio Selector<br>
 * You have to handle some nio events and io exceptions your self<br>
 * It does the same in vproxy as `AE` does in redis
 */
package vproxy.selector;